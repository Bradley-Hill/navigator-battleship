function createGameboard(size:number) {
  //TODO: gameboard should be an array of arrays coordinates based on size parameter
  const gameboard:[][] =  
  return {};
}

module.exports = createGameboard;
